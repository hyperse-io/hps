{
  "name": "@hyperse/hps-plugin-update",
  "version": "0.1.1",
  "homepage": "https://github.com/hyperse-io/hps",
  "repository": {
    "type": "git",
    "url": "https://github.com/hyperse-io/hps",
    "directory": "packages/hps-plugin-update"
  },
  "license": "SEE LICENSE IN FILE 'LICENSE'",
  "author": {
    "name": "Hyperse",
    "url": "https://github.com/hyperse-io"
  },
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./index.js"
    },
    "./package.json": "./package.json"
  },
  "types": "./dist/index.d.ts",
  "files": [
    "bin/",
    "dist/",
    "index.js",
    "README.md",
    "CHANGELOG.md",
    "package.json"
  ],
  "scripts": {
    "build": "rimraf dist && tsc -p ./tsconfig.build.json",
    "?build-release": "When https://github.com/atlassian/changesets/issues/432 has a solution we can remove this trick",
    "build-release": "yarn build && rimraf ./_release && yarn pack && mkdir ./_release && tar zxvf ./package.tgz --directory ./_release && rm ./package.tgz",
    "clean": "rimraf --no-glob ./dist ./_release ./coverage ./tsconfig.tsbuildinfo",
    "clean:test-snap": "rimraf --no-glob ./tests/__snapshots__",
    "lint": "eslint .",
    "fix-all-files": "eslint . --fix",
    "test": "yarn clean:test-snap && run-s test-unit",
    "test-unit": "yarn clean:test-snap && vitest run",
    "test-coverage": "yarn clean:test-snap && vitest run --coverage",
    "test-unit-watch": "yarn clean:test-snap && vitest watch --ui",
    "typecheck": "tsc --project ./tsconfig.json --noEmit"
  },
  "dependencies": {
    "@hyperse/config-loader": "^1.0.9",
    "@manypkg/get-packages": "^3.1.0",
    "has-yarn": "^4.0.0",
    "npm-check-updates": "^19.6.6",
    "package-up": "^5.0.0"
  },
  "devDependencies": {
    "@hyperse/eslint-config-hyperse": "^1.7.1",
    "@hyperse/wizard": "^1.0.6",
    "@types/node": "^25.5.0",
    "@vitest/coverage-istanbul": "^4.1.2",
    "@vitest/ui": "^4.1.2",
    "eslint": "^10.1.0",
    "npm-run-all": "^4.1.5",
    "rimraf": "^6.1.3",
    "typescript": "^6.0.3",
    "vite": "^8.0.3",
    "vitest": "^4.1.2"
  },
  "peerDependencies": {
    "@hyperse/wizard": ">=1.0.6"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "publishConfig": {
    "access": "public",
    "directory": "_release/package"
  }
}
